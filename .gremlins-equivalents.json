[
  {
    "file": "internal/rclone/classify.go",
    "line": 74,
    "column": 35,
    "type": "ARITHMETIC_BASE",
    "reason": "64*1024 is only the scanner's initial buffer capacity. The mutation makes it 64/1024 = 0; bufio.Scanner then allocates its default 4096-byte buffer on first use and doubles it up to the same 1 MiB maximum, so every input scans identically. Confirmed empirically: with the mutation hand-applied the full package suite, including the 100 KiB line case, still passes."
  },
  {
    "file": "internal/transfer/cycle.go",
    "line": 124,
    "column": 19,
    "type": "CONDITIONALS_BOUNDARY",
    "reason": "res.Outcome > worst vs >= worst: the only extra case is equality, where the body assigns worst = res.Outcome to a variable that already holds that value. The recorded outcome is identical for every sequence of copy results. Confirmed empirically: with the mutation hand-applied the full package suite still passes."
  }
]
